import { createMuiTheme } from "@material-ui/core/styles";

const MainTheme = createMuiTheme({
  palette: {
    primary: {
      main: "#424242",
    },
    secondary: {
      main: "#FFE082",
    },
  },
});
